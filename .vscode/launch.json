{
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug",
            "url": "http://localhost:5173",
            "webRoot": "${workspaceFolder}"
        }
    ]
}